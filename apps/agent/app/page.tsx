import {getChatGPTUser,chatGPTSignInPath} from './chatgpt-auth';
import Connections from './connections';
import CopilotPanel from './copilot-panel';
export const dynamic='force-dynamic';
export default async function Home(){const user=await getChatGPTUser();return <main className="shell"><header><span className="brand">p.</span><strong>Primtal</strong><span className="pill">Private setup</span></header><section className="intro"><p className="eyebrow">CONNECTIONS</p><h1>Connect your bot.</h1><p>Ambiguous for conversations. OpenRouter for the model.</p></section><section className="card"><h2>Provider keys</h2><p className="muted">Keys are encrypted on the server. Saved values are never returned to the page.</p>{user?<Connections/>:<a className="primary" href={chatGPTSignInPath('/')} target="_top">Sign in to connect Primtal</a>}</section>{user&&<CopilotPanel/>}<section className="notice" style={{marginTop:24}}><strong>Implementation in progress</strong><p>This page validates and stores credentials. Daily prompts, team pairing and the remote demo launch are still being connected. Saving keys does not send messages or modify calendars.</p></section><footer>Primtal · Team demo setup</footer></main>}
